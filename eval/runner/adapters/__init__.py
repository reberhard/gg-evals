"""Reference adapters. One subpackage per eval. Each adapter is a minimal,
runnable, no-config implementation suitable for benchmark comparison.

Production-grade variants ship in gg-stack with operator-specific config.
"""
